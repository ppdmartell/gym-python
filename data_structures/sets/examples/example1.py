s1 = {1, 2, 3, 4, 5}
print(type(s1))


# creating a seet using the set function
s2 = set([1, 2, 3, 4, 5, 6, 6])     # a set {1, 2, 3, 4, 5, 6} will be created, without the repeated 6
print(s2)

# sets can't get their elements accessed by index like list and tuples. Use a for loop for it.
